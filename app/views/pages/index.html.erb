<div class="adm-directory dir-page">
  <div class="dir-search-container adm-input">
    <input type="text" class="dir-search" placeholder="Filter">
  </div>
  <div class="dir-listing">
  <% @pages_sorted_alpha.each do |letter, page| %>
    <div class="section-alpha">
      <div class="alpha-letter letter-<%= letter %>">
        <%= letter.upcase %>
      </div>
      <ul class="alpha-list">
        <% page.each do |p| %>
          
          <li class="alpha-list-item item-id-<%=p.id%>" data-item-id="<%= p.id %>">
            <%= link_to p.name, page_path(p), :remote => true %>
          </li>
        
        <% end %>
      </ul>
    </div>
  <% end %>
  </div>
</div>