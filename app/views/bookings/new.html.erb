<div class="adm-midfloat-box">
<%= form_for @booking do |f| %>
  <div class="adm-input">
  	<%= f.label :date, "Date:" %>
  	<%= f.text_field :date,
  				value: @booking_date.strftime("%e %B %Y"),
  				disabled: true, :class => "adm-date-query" %>
  </div>
<!-- need to find a way to pass in client and page data for a drop down menu -->
	<!-- <#%= f.label :name_client %> -->
	<!-- <#%= f.text_field :name_client %> -->
<!-- 
	<#%= f.label :page_name %>
	<#%= f.text_field :page_name %> -->

	<%= f.submit "Book Now", :class => "adm-button adm-button-main"%>

<% end %>
</div>