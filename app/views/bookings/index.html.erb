Welcome to the bookings page!!
<%= render "shared/alertnotice" %>
<%= link_to 'Look Up', query_bookings_path %>

<br>
<br>

<% @bookings.each do |b| %>
	<%= b.client.name %>
	<%= b.date.to_s %>
  <br>
  <hr>
<% end %>

